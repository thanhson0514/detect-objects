export interface typeDocuments {
  id: number;
  category: string;
  title: string;
}

export interface typeDetailDocument {
  id: number;
  category_id: number;
  title: string;
  image: string;
}
